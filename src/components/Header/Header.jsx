import React from 'react';
import NavBar from './NavBar/NavBar';

const Header = () => {
    return (
        <header className='py-5'>
            <NavBar></NavBar>
        </header>
    );
};

export default Header;